export const APP_HOST = 'localhost';

export const ADMIN_FRONTEND_PORT = 3000;
export const ADMIN_BACKEND_PORT = 5050;
export const REPLAY_SERVER_PORT = 5051;

export const ADMIN_FRONTEND_ORIGIN = `http://${APP_HOST}:${ADMIN_FRONTEND_PORT}`;
export const ADMIN_BACKEND_ORIGIN = `http://${APP_HOST}:${ADMIN_BACKEND_PORT}`;
export const REPLAY_SERVER_ORIGIN = `http://${APP_HOST}:${REPLAY_SERVER_PORT}`;
